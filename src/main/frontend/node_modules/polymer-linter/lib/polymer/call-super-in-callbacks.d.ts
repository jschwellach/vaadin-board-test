import { Document, Warning } from 'polymer-analyzer';
import { Rule } from '../rule';
export declare class CallSuperInCallbacks extends Rule {
    code: string;
    description: string;
    constructor();
    check(document: Document): Promise<Warning[]>;
}
